VITE_API_PROXY_TARGET=http://127.0.0.1:8911
# Optional legacy module origin for standalone development.
# VITE_MOD_ORIGIN=http://127.0.0.1:5891
# Optional paired Numa Agent entry. Cross-origin targets are rejected unless
# their exact origin is listed in VITE_NUMA_AGENT_ALLOWED_ORIGINS.
# VITE_NUMA_AGENT_URL=https://numa.example/
# VITE_NUMA_AGENT_ALLOWED_ORIGINS=https://numa.example
