import WorkbenchClient from './WorkbenchClient'

export const dynamic = 'force-dynamic'
export const metadata = { title: '研究工作台' }

export default function WorkbenchPage() {
  return <WorkbenchClient />
}
