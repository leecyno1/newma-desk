# 历史文档归档

本目录存放项目早期阶段（一代全栈原型、二代 frontend/backend 分离架构、三代证据驱动重构初期）的阶段性文档。

这些文档描述的架构（Wind 数据源、旧 `frontend/` 目录、pgvector、评分筛选系统等）已不在当前主干中，仅作历史追溯用途，不作为现状依据。

当前架构见根目录 `ARCHITECTURE.md`，产品边界见 `CONTEXT.md`，交接状态见 `QODER_HANDOFF.md`。
