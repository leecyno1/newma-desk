import { readFileSync } from 'node:fs'

const read = (path) => readFileSync(path, 'utf8')
const assertIncludes = (content, needle, label) => {
  if (!content.includes(needle)) {
    throw new Error(`${label} missing: ${needle}`)
  }
}

const searchPage = read('app/(dashboard)/reports/search/page.tsx')
const searchRoute = read('app/api/reports/search/route.ts')

assertIncludes(searchPage, 'report-search-buy-before-results', 'report search buy-before results hook')
assertIncludes(searchPage, 'report-search-sales-rule-gate', 'report search sales-rule gate card')
assertIncludes(searchPage, 'report-search-risk-level-policy', 'report search R1-R5 policy badge')
assertIncludes(searchPage, 'report-search-risk-level-policy-card', 'report search old R1-R5 policy warning card')
assertIncludes(searchPage, 'report-search-purchase-decision', 'report search purchase decision card')
assertIncludes(searchPage, 'report-search-review-freshness', 'report search shortlist review freshness card')
assertIncludes(searchPage, 'report-search-next-action', 'report search next action card')
assertIncludes(searchPage, 'report-search-replay-evidence-gate', 'report search replay evidence gate card')
assertIncludes(searchPage, 'searchResultTodayUsability', 'report search computes today usability for every hit')
assertIncludes(searchPage, 'searchTodayUsabilityRows', 'report search builds today usability rows')
assertIncludes(searchPage, 'searchTodayUsabilityAudit', 'report search groups today usability audit lanes')
assertIncludes(searchPage, 'downloadSearchTodayUsabilityTsv', 'report search can download today usability TSV')
assertIncludes(searchPage, "['今日结论', '报告标题', '报告类型', '匹配度', '下一步', '处理入口', '硬边界']", 'report search today usability TSV columns')
assertIncludes(searchPage, 'report-search-today-usability-audit', 'report search renders today usability audit')
assertIncludes(searchPage, 'report-search-today-usability-download', 'report search renders today usability TSV download')
assertIncludes(searchPage, 'report-search-result-today-usability', 'report search renders per-result today usability')
assertIncludes(searchPage, '搜索结果今日沿用判断', 'report search today usability title')
assertIncludes(searchPage, '只作历史回看 / 需重跑 / 今天可沿用研究', 'report search today usability lanes')
assertIncludes(searchPage, '搜索命中 ≠ 今日可用', 'report search warns against stale search hit reuse')
assertIncludes(searchPage, '历史报告不能覆盖今日销售平台/R1-R5/费率/申赎/限购/净值回放', 'report search hard boundary for stale evidence')
assertIncludes(searchPage, '硬证据不足时只给补证或重跑动作，不给申赎指令', 'report search no-trading hard boundary')
assertIncludes(searchPage, '销售规则、R1-R5、费率、申赎、限购、净值回放或测算证据任一缺失', 'report search downgrade condition lists required evidence')
assertIncludes(searchPage, '测算证据门禁：', 'report search exposes replay evidence gate label')
assertIncludes(searchPage, '搜索命中的旧横评未记录测算证据门禁', 'report search downgrades legacy comparison reports without replay gates')
assertIncludes(searchPage, '重跑真实回放横评', 'report search links legacy comparison reports to real replay rerun')
assertIncludes(searchPage, '先补销售规则', 'report search sales-rule next action')
assertIncludes(searchPage, 'isReviewQueueGate', 'report search detects active review queue gates')
assertIncludes(searchPage, '处理复查队列', 'report search labels active review queue action')
assertIncludes(searchPage, '复查队列未清零，仅供回看', 'report search shows review queue invalidation')
assertIncludes(searchPage, '复查队列待处理', 'report search shows review queue missing item fallback')
assertIncludes(searchPage, '补 R1-R5 来源', 'report search R1-R5 next action')
assertIncludes(searchPage, 'riskLevelSourceQueueHref', 'report search builds R1-R5 source queue href')
assertIncludes(searchPage, '进入 R1-R5 来源补证队列', 'report search links old R1-R5 reports to source queue')
assertIncludes(searchPage, '短名单复查时效', 'report search displays shortlist review freshness')
assertIncludes(searchPage, 'reviewFreshnessDetail', 'report search renders review freshness detail')
assertIncludes(searchPage, 'shortlistReviewFreshness', 'report search derives shortlist review freshness')
assertIncludes(searchPage, 'poolReviewHref', 'report search builds pool review href')
assertIncludes(searchPage, '更新复查结论', 'report search prioritizes overdue review refresh action')
assertIncludes(searchPage, '回研究清单更新复查', 'report search review freshness card links back to pool')
assertIncludes(searchPage, '不保存或沿用正式研究结论', 'report search hard-gate discipline')
assertIncludes(searchPage, '重跑横向比较', 'report search comparison rerun action')
assertIncludes(searchPage, 'resultPurchasePlan', 'report search derives purchase-plan context')
assertIncludes(searchPage, 'resultPlannedAmount', 'report search derives planned amount context')
assertIncludes(searchPage, 'appendPurchaseContext', 'report search uses purchase context helper')
assertIncludes(searchPage, "params.set('plannedAmount', String(plannedAmount))", 'report search links carry planned amount')
assertIncludes(searchPage, "params.set(purchasePlan === 'lump_sum' ? 'lumpSumAmount' : 'monthlyAmount', String(plannedAmount))", 'report search links carry amount alias')
assertIncludes(searchPage, "appendPurchaseContext(`/analysis/comparison?codes=${encodeURIComponent(relatedCodes.join(','))}&autoReplay=1`, result)", 'report search comparison rerun carries purchase plan and amount')
assertIncludes(searchPage, "appendPurchaseContext(`/funds/${encodeURIComponent(result.targetId)}`, result)", 'report search fund detail action carries purchase plan and amount')
assertIncludes(searchPage, "appendPurchaseContext(`/pools?poolId=${encodeURIComponent(result.targetId)}&status=candidate`, result)", 'report search pool action carries purchase plan and amount')
assertIncludes(searchRoute, 'currentSalesRuleGate', 'report search API preserves current sales-rule gate')
assertIncludes(searchRoute, '/api/reports', 'report search API reuses reports endpoint with active current gates')
assertIncludes(searchRoute, 'riskLevelGatePolicy', 'report search API preserves R1-R5 gate policy')
assertIncludes(searchRoute, 'decisionSummary', 'report search API preserves decision summary')
assertIncludes(searchRoute, 'sourceDecisionCards', 'report search API type preserves source decision cards')
assertIncludes(searchRoute, 'reviewFreshnessLabel', 'report search API type preserves shortlist review freshness')
assertIncludes(searchRoute, 'replayEvidenceGateStatus', 'report search API type preserves replay evidence gate status')
assertIncludes(searchRoute, 'replayEvidenceGateMissingEvidence', 'report search API type preserves replay evidence gate missing items')
assertIncludes(searchRoute, 'actionHref', 'report search API preserves report action href')
assertIncludes(searchRoute, 'purchasePlan', 'report search API preserves report purchase-plan context')
assertIncludes(searchRoute, 'plannedAmount', 'report search API preserves report planned amount context')

console.log('OK report search surfaces buy-before gate and next actions')
