# Cursor Rules for 周期判断系统

## 项目概述
这是一个基于多周期理论的量化投资决策支持系统，通过全球经济指标的周期分析为资产配置提供数据驱动的投资建议。

## 开发规范

### 代码风格
- 使用Python 3.9+
- 遵循PEP 8代码规范
- 使用类型提示 (Type Hints)
- 函数和类必须有详细的docstring
- 变量命名使用snake_case，类名使用PascalCase

### 模块设计原则
- 单一职责原则：每个模块只负责一个功能
- 依赖注入：通过配置文件管理依赖关系
- 接口分离：定义清晰的接口和抽象类
- 错误处理：使用自定义异常类，提供详细错误信息

### 数据处理规范
- 所有时间序列数据使用pandas DataFrame
- 日期格式统一使用ISO 8601 (YYYY-MM-DD)
- 数据验证：输入数据必须经过验证
- 缓存机制：频繁访问的数据使用缓存

### 周期分析规范
- 周期长度单位统一使用月份
- 周期位置使用0-1标准化表示
- 支持6种标准周期：康波(600)、地产(200)、资本(100)、基钦(42)、信用(20)、年度(12)
- 周期状态：扩张前期、扩张后期、收缩前期、收缩后期

### 指标体系规范
- 五大维度：海外面、资金面、基本面、政策面、情绪面
- 三类指标：先行、同步、滞后
- 指标标准化：使用Z-score或Min-Max标准化
- 指标权重：支持动态权重调整

### 测试规范
- 单元测试覆盖率 > 80%
- 集成测试覆盖主要业务流程
- 性能测试：数据处理性能基准
- 数据质量测试：验证数据完整性和准确性

### 文档规范
- 每个模块包含README.md
- API文档使用Sphinx自动生成
- 配置文件包含详细注释
- 变更日志记录重要更新

### 性能要求
- 数据采集：支持并发请求，限制API调用频率
- 内存使用：大数据集使用分块处理
- 计算效率：周期分析算法优化，支持向量化计算
- 响应时间：预测结果生成 < 5秒

### 安全规范
- API密钥通过环境变量管理
- 数据库连接使用连接池
- 输入验证：防止SQL注入和XSS攻击
- 日志记录：记录关键操作，不记录敏感信息

### 部署规范
- 使用Docker容器化部署
- 配置文件与代码分离
- 支持多环境配置（开发、测试、生产）
- 数据库迁移脚本版本控制

## 开发工作流
1. 功能开发前先编写测试用例
2. 代码提交前运行完整测试套件
3. 重要功能变更需要代码审查
4. 定期进行性能基准测试
5. 文档与代码同步更新

## 依赖管理
- 核心依赖：pandas, numpy, scipy, scikit-learn
- 数据源：akshare, yfinance, fredapi
- 可视化：plotly, matplotlib, seaborn
- 数据库：SQLAlchemy, sqlite3/postgresql
- 测试：pytest, pytest-cov
- 开发工具：black, flake8, mypy 